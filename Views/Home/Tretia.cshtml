@using myMVC.Models;

<h3>Third pagea</h3>

<h4>List books</h4>

@foreach (var b in Model)
{
    <p>Id: @b.Id</p>
    <p>Názov: @b.Name</p>
    <p>Autor: @b.Author</p>
}

<p>Dátum: @ViewBag.Date</p>
<br />
<button type="button" class="btn btn-sm btn-default" onclick="ButtonClick()">Change background color</button>

<script type="text/javascript">

    function ButtonClick()
    {
            $("div").css({ "background-color": "blue" });
            $(document).ready(function () {
                $("p").css({ "color": "#000" });
            });
    }

</script>
